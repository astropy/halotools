[build-system]

requires = ["setuptools>=42.0.0",
            "setuptools_scm",
            "wheel",
            "oldest-supported-numpy",
            "cython==0.29.32",
            "extension-helpers"]

build-backend = 'setuptools.build_meta'
